@namespace Material.Blazor

@inherits InputComponent<int>
@typeparam TItem

<div class="mb-carousel @(@class)"
     style="@style">

    <div class="mb-carousel__upper">
        <div class="mb-carousel-nav-panel" style="order: 2;">
            <MBSlidingContent @bind-Value="@CurrentIndex"
                              Context="item"
                              ItemIndex="@Value"
                              Items="@Items"
                              @ref="@SlidingContent">

                <Content>
                    @Content(item)
                </Content>
            </MBSlidingContent>
        </div>

        <MBFloatingActionButton Icon="navigate_before"
                                style="order: 1;"
                                @onclick="NavigatePrevious" />

        <MBFloatingActionButton Icon="navigate_next"
                                style="order: 3;"
                                @onclick="NavigateNext" />
    </div>

    <MBFloatingActionButton @key="@Play.ToString()"
                            Icon="@PlayIcon"
                            @onclick="@(() => PlayStop())" />

    <div class="mb-carousel__lower">

    </div>

</div>