# pnpm >= 10 no longer reads pnpm settings from this file (see pnpm-workspace.yaml).
# engine-strict is kept because plain npm reads it, making 'npm install' hard-fail
# against the engines guard in package.json instead of just warning.
engine-strict = true
