@namespace BlazorMdc.Demo

@inject IPMdcAnimatedNavigationManager PMdcAnimatedNavigationManager

@page "/"

<h1 class="@MdcTypography.Heading3">Hello, world!</h1>

<p>This sample demonstrates the use of RazorComponents.MaterialDesign components.</p>

<p>Currently executing on <strong>@runtime</strong> built in <strong>@mode</strong> mode.</p>

<p><PMdcNumericIntField @bind-Value="@PMdcAnimatedNavigationManager.Configuration.AnimationTime" Label="Page animation time (ms)" NumericFormat="N0" Min="50" Max="3000" /></p>

<p><MdcCheckbox @bind-Value="@PMdcAnimatedNavigationManager.Configuration.ApplyAnimation" Label="Apply animiations to page navigation" /></p>

<div style="padding: 1rem; background-color: lightgray;">
    <p>The checkbox and toggle icon button both bind to the same value. Press the toggle, then checkbox then toggle again and ... crash!</p>

    <p><MdcCheckbox @bind-Value="@commonBool" Label="Checkbox and Toggle both bind to same value" /></p>

    <p><MdcIconButtonToggle @bind-Value="@commonBool" IconOn="@MdcIcons.Icon__directions_run" IconOff="@MdcIcons.Icon__airline_seat_individual_suite" /></p>

    <p>@commonBool</p>
</div>

<p style="margin-top: 80rem;">&nbsp;</p>

@code {
    bool commonBool = false;
}
