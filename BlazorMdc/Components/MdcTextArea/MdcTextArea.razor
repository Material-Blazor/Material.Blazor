@namespace BMdc
@inherits BBase.InputComponentBase<string>


<div @ref="ElementReference"
     @attributes="@AttributesToSplat(BBase.SplatType.ClassAndStyleOnly)">
    
    @if (AppliedTextInputStyle == BEnum.TextInputStyle.Filled || AppliedTextInputStyle == BEnum.TextInputStyle.FullWidth)
    {
        <div class="mdc-text-field__ripple"></div>
    }

    <textarea id="@id"
              class="mdc-text-field__input @AppliedTextInputStyleClass @FieldClass"
              @bind="@ReportingValue"
              aria-labelledby="@labelId"
              rows="@Rows"
              cols="@Cols"
              disabled="@Disabled"
              @attributes="@AttributesToSplat(BBase.SplatType.ExcludeClassAndStyle)" />

    @if (AppliedTextInputStyle == BEnum.TextInputStyle.Outlined)
    {
        <div class="mdc-notched-outline">
            <div class="mdc-notched-outline__leading"></div>
            <div class="mdc-notched-outline__notch">
                <label class="mdc-floating-label @FloatingLabelClass" for="@id" id="@labelId">@Label</label>
            </div>
            <div class="mdc-notched-outline__trailing"></div>
        </div>
    }
    else
    {
        @if (AppliedTextInputStyle == BEnum.TextInputStyle.Filled)
        {
            <label class="mdc-floating-label @FloatingLabelClass" for="@id" id="@labelId">@Label</label>
        }

        <div class="mdc-line-ripple"></div>
    }
</div>