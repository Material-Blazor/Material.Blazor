@namespace BlazorMdc

@typeparam TItem

@inherits InputComponentFoundation<int>


@if (Icon is null && Label is null)
{
    <MTTabBar @bind-Value="@ReportingValue"
              Items="@Items"
              Context="item"
              StackIcons="@StackIcons"
              Disabled="@AppliedDisabled"
              Density="@Density"
              GetKeysFunc="@GetKeysFunc">

    </MTTabBar>
}
else if (Label is null)
{
    <MTTabBar @bind-Value="@ReportingValue"
              Items="@Items"
              Context="item"
              StackIcons="@StackIcons"
              Disabled="@AppliedDisabled"
              Density="@Density"
              GetKeysFunc="@GetKeysFunc">

        <Icon>@Icon(item)</Icon>
    </MTTabBar>
}
else if (Icon is null)
{
    <MTTabBar @bind-Value="@ReportingValue"
              Items="@Items"
              Context="item"
              StackIcons="@StackIcons"
              Disabled="@AppliedDisabled"
              Density="@Density"
              GetKeysFunc="@GetKeysFunc">

        <Label>@Label(item)</Label>
    </MTTabBar>
}
else
{
    <MTTabBar @bind-Value="@ReportingValue"
              Items="@Items"
              Context="item"
              StackIcons="@StackIcons"
              Disabled="@AppliedDisabled"
              Density="@Density"
              GetKeysFunc="@GetKeysFunc">

        <Label>@Label(item)</Label>
        <Icon>@Icon(item)</Icon>
    </MTTabBar>
}


<MTSlidingContent Items="@Items"
                  ItemIndex="@Value"
                  Context="item">

    <Content>
        @Content(item)
    </Content>
</MTSlidingContent>