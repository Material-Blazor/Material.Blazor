@namespace BlazorMdc
@inherits InputComponentFoundation<int>


<MTNumericDoubleField @attributes="@AttributesToSplat()"
                      TextInputStyle="TextInputStyle"
                      Label="@Label"
                      Prefix="@Prefix"
                      Suffix="@Suffix"
                      LeadingIcon="@LeadingIcon"
                      TrailingIcon="@TrailingIcon"
                      IconFoundry="@IconFoundry"
                      NumericFormat="@NumericFormat"
                      NumericSingularFormat="@NumericSingularFormat"
                      FocussedMagnitude="@FocussedMagnitude"
                      UnfocussedMagnitude="@UnfocussedMagnitude"
                      Min="@Min"
                      Max="@Max"
                      DecimalPlaces="0"
                      Disabled="@Disabled"
                      @bind-Value="@DblVal" />
