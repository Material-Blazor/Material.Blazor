@namespace BMdcPlus
@inherits ComponentFoundation


<div @attributes="AttributesToSplat()">

    <div class="bmdc-paginator">
        <span>@ItemsText</span>

        <div>
            <BMdc.IconButtonToggle @ref="@IconButtonToggle" @bind-Value="@ToggleOn" @onclick="@OnMenuToggleAsync" IconOn="arrow_drop_up" IconOff="arrow_drop_down" />
            <div class="mdc-menu-surface--anchor">
                <BMdc.Menu @ref="Menu">
                    @foreach (var itemsPerPage in ItemsPerPageSelection)
                    {
                        <BMdc.ListItem @key="@itemsPerPage" Text="@itemsPerPage.ToString("G0")" OnClick="@(_ => OnMenuItemClick(itemsPerPage))" />
                    }
                </BMdc.Menu>
            </div>
        </div>

        <div class="bmdc-paginator__position-text">
            @* This hidden element serves to give the actual position text a constant width equal to the maximum width that may be required *@
            <span class="bmdc-paginator__position-text-hidden">@MaxPositionText</span>
            <span>@PositionText</span>
        </div>

        <BMdc.IconButton Icon="chevron_left" @onclick="@OnPreviousClick" Disabled="@PreviousDisabled" />

        <BMdc.IconButton Icon="chevron_right" @onclick="@OnNextClick" Disabled="@NextDisabled" />
    </div>
</div>